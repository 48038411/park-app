<!--index.wxml-->
<view>
  <map id="map" subkey="{{subkey}}" layer-style="1" longitude="{{longitude}}" latitude="{{latitude}}" markers="{{markers}}" show-location="true" style="position: fixed;width: 100%;height: 100%;"></map>
</view>
